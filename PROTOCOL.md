# SproutOS deployment-template plugin protocol

The normative protocol document ships inside the canonical crate at
[`packages/sprout-template-protocol/PROTOCOL.md`](packages/sprout-template-protocol/PROTOCOL.md).
Keeping the normative document inside the package ensures crates.io releases contain the same
contract as the Rust types, schemas, and compatibility fixtures they publish.
